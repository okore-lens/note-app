import { View, Text } from "react-native";
import React from "react";

const ProfileScreen = () => {
	return (
		<View className="flex-1 bg-[#252525] items-center">
			<Text className="text-white">ProfileScreen</Text>
		</View>
	);
};

export default ProfileScreen;
